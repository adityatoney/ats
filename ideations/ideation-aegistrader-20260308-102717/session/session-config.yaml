# Session Configuration
# Produced by the Plan action, consumed by the Ideate action.

concept_seed: "docs/Concept_1.md and docs/PRD.md — Multi-Agent Financial Paper Trading Research Platform (Project AegisTrader)"
concept_slug: "aegistrader"

# Lineage (populated for continuations, null for new sessions)
parent_session: null
parent_version: null
continuation_focus: null
branch_point: null

# Depth
depth:
  level: "standard"          # quick | standard | deep | exhaustive
  max_rounds: null           # null = use level default
  confidence_threshold: null # null = use level default

# Outputs (Tier 2 — Tier 1 session artifacts always produced)
outputs:
  predefined:
    distribution_page: true
    results_pdf: true
    capsule_pdf: true
    presentation: true
    infographic_images: true
  custom: []

# Research
research:
  mode: "parallel"
  topics:
    - "Competitive landscape: existing AI trading platforms, agent-based backtesting tools, AI-assisted strategy evolution products"

# Session Focus
focus: "Both equally — explore new dimensions (UX paradigms, product positioning, differentiation, novel agent interaction patterns) while also challenging and refining existing architectural and PRD decisions"
